# mysql
spring:
  datasource:
    #MySQL配置
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://**********:3306/easyproject?autoReconnect=true&useUnicode=true&createDatabaseIfNotExist=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: ****

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.jjle.pojo

server:
  port: 9000
